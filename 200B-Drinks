#include<iostream>
using namespace std;
int main()
{
      int n,p,i,s=0;
      cin >> n;
      for(i=0; i<n; i++)
      {
         cin >> p;
         s+=p;
      }
      cout << (float)s/n << endl;
return 0;
}
